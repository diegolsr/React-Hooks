export const initialState = { };

const anotherExampleReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};

export default anotherExampleReducer;